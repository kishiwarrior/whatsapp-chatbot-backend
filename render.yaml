services:
  - type: web
    name: whatsapp-chatbot-backend
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/whatsapp-bot-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
    healthCheckPath: /webhook/health
